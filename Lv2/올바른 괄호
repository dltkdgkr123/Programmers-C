/*
  stack문제
  괄호개수 불일치시 false/ cnt = -1일시 닫는괄호가 먼저 나온 것이므로 false
*/

#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

// 파라미터로 주어지는 문자열은 const로 주어집니다. 변경하려면 문자열을 복사해서 사용하세요.
bool solution(const char* s) {
    bool answer = true;
    int len = strlen(s);
    char* p = (char*)malloc(len);
    strcpy(p, s);
    
    int cnt = 0;

    for (int i = 0; i < len; i++)
    {
        if (p[i] == '(')
            cnt++;
        else
            cnt--;
        
        if(cnt==-1)
            return false;
    }

    if (cnt)
        answer = false;

    return answer;
}
